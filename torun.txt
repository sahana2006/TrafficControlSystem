# Emergency Vehicle Traffic Control — run commands

pip install -r requirements.txt

$env:SUMO_HOME = "C:\Program Files (x86)\Eclipse\Sumo"
$env:PATH = "$env:SUMO_HOME\bin;$env:PATH"

# First-time setup
python main.py --build

# Rule-Based controller
python main.py --controller rule_based

# DQN controller (trains online, saves model to results/models/dqn.pt)
python main.py --controller dqn

# Compare Rule-Based vs DQN + generate comparison graphs
python main.py --compare

# GUI + logs + graphs (zoomed-out, tracks red EV_1)
python main.py --gui --controller dqn --log-interval 5 --ev-log-interval 1

# Full comparison with GUI
python main.py --compare --gui --end 900

# SUMO-GUI only (no Python controller)
sumo-gui -c simulation.sumocfg
